package com.videowebsite.VideoWebsite.contact;

/**
 * ContactRepository was previously a Spring Data JPA repository.
 * We are now persisting Contact objects in Firestore. Keep this
 * placeholder class to avoid compilation errors in older build artifacts.
 */
public final class ContactRepository {
	private ContactRepository() {}
}
